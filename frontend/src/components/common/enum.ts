export enum direction{
    left =  'left',
    right = 'right',
    top = 'top',
    bottom = 'bottom'
}

export enum eventKey{
    tab =  'Tab',
    shift = 'Shift',
}

export enum keyEvent{
    keyDown =  'keydown'
}